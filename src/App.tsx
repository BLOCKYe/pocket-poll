import React from 'react';

import firebase from "./core/config/firebase";

const App: React.FC = () => {

    return (
        <div>
            let's get the party started
        </div>
    );
}

export default App;
